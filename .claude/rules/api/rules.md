# src/api/ のルール

`src/api/` 配下のファイルを編集・作成するときは以下を必ず守る。

- すべての引数・戻り値に TypeScript 型を明示する（`any` 禁止）
- レスポンス型は `src/types/` に定義し、ここで import して使う
- エラーハンドリングは呼び出し元（composable）に委ねる（API 関数は例外を throw する）
- Axios インスタンスは既存の共通インスタンスを再利用する（新規作成しない）
